# Figma 组件转代码映射规则

## 🎯 组件映射表

### 基础 UI 组件映射

| Figma 元素类型 | React 组件 | 示例代码 |
|--------------|-----------|---------|
| Button (Auto Layout) | `<button>` 或 `<Button>` | `<button className={styles.btn}>文本</button>` |
| Text Input | `<input>` 或 `<Input>` | `<input type="text" className={styles.input} />` |
| Card/Frame (带阴影) | `<div>` + Card 组件 | `<Card className={styles.card}>内容</Card>` |
| Image | `<img>` 或 `<Image>` | `<img src={url} alt="描述" className={styles.image} />` |
| Icon (Vector) | SVG 组件或 Icon 组件 | `<Icon name="user" className={styles.icon} />` |
| Dropdown/Select | `<select>` 或 `<Select>` | `<Select options={options} />` |
| Checkbox | `<input type="checkbox">` | `<input type="checkbox" className={styles.checkbox} />` |
| Radio | `<input type="radio">` | `<input type="radio" className={styles.radio} />` |
| Modal/Dialog | Modal 组件 | `<Modal visible={visible} onClose={handleClose}>内容</Modal>` |
| Table | `<table>` 或 `<Table>` | `<Table dataSource={data} columns={columns} />` |
| Form | `<form>` 或 `<Form>` | `<Form onFinish={handleSubmit}>...</Form>` |

### 布局组件映射

| Figma 布局类型 | React 组件 | 说明 |
|--------------|-----------|------|
| Frame (Flex Horizontal) | `<div className={styles.flexRow}>` | 水平布局 |
| Frame (Flex Vertical) | `<div className={styles.flexCol}>` | 垂直布局 |
| Grid | `<div className={styles.grid}>` | 网格布局 |
| Auto Layout | Flexbox 或 Grid | 自适应布局 |

### 特殊组件识别

#### 1. 导航组件 (Navigation)
```tsx
// Figma: 水平菜单栏
<nav className={styles.nav}>
  <ul className={styles.menu}>
    <li><a href="/home">首页</a></li>
    <li><a href="/about">关于</a></li>
  </ul>
</nav>

// Figma: 侧边栏菜单
<aside className={styles.sidebar}>
  <Menu items={menuItems} />
</aside>
```

#### 2. 数据展示组件 (Data Display)
```tsx
// Figma: 卡片列表
<div className={styles.cardList}>
  {items.map(item => (
    <Card key={item.id} {...item} />
  ))}
</div>

// Figma: 表格
<Table 
  columns={columns}
  dataSource={data}
  pagination={{ pageSize: 10 }}
/>
```

#### 3. 表单组件 (Form)
```tsx
// Figma: 表单容器
<Form onFinish={handleSubmit} layout="vertical">
  <Form.Item label="用户名" name="username">
    <Input placeholder="请输入用户名" />
  </Form.Item>
  <Form.Item label="密码" name="password">
    <Input.Password placeholder="请输入密码" />
  </Form.Item>
  <Form.Item>
    <Button type="primary" htmlType="submit">提交</Button>
  </Form.Item>
</Form>
```

#### 4. 反馈组件 (Feedback)
```tsx
// Figma: 提示消息
<Message type="success">操作成功</Message>

// Figma: 加载状态
<Spin spinning={loading}>
  <Content />
</Spin>

// Figma: 空状态
<Empty description="暂无数据" />
```

## 🔍 组件识别规则

### 1. 按钮识别
- **特征**: 有明确交互提示（颜色、边框、阴影）
- **文本**: 通常为动词，如"提交"、"取消"、"确认"
- **尺寸**: 常见的按钮高度（32px, 36px, 40px）

### 2. 输入框识别
- **特征**: 有边框，可能有占位符文本
- **标签**: 上方或左侧有标签文本
- **验证**: 可能有错误提示样式

### 3. 卡片识别
- **特征**: 有阴影或边框，圆角
- **内容**: 包含标题、图片、描述等组合
- **交互**: 可能有 hover 效果

### 4. 表格识别
- **特征**: 网格布局，有表头
- **数据**: 多行多列结构
- **功能**: 可能有排序、筛选、分页

## 📦 组件生成模板

### 基础 Button 组件
```tsx
// components/common/Button.tsx
import React from 'react';
import styles from './Button.module.css';

export interface ButtonProps {
  children: React.ReactNode;
  type?: 'primary' | 'default' | 'danger' | 'link';
  size?: 'small' | 'medium' | 'large';
  disabled?: boolean;
  loading?: boolean;
  onClick?: () => void;
  className?: string;
}

export const Button: React.FC<ButtonProps> = ({
  children,
  type = 'default',
  size = 'medium',
  disabled = false,
  loading = false,
  onClick,
  className = '',
}) => {
  const classNames = [
    styles.button,
    styles[type],
    styles[size],
    disabled && styles.disabled,
    loading && styles.loading,
    className,
  ]
    .filter(Boolean)
    .join(' ');

  return (
    <button
      className={classNames}
      disabled={disabled || loading}
      onClick={onClick}
    >
      {loading && <span className={styles.spinner} />}
      {children}
    </button>
  );
};
```

### 基础 Card 组件
```tsx
// components/common/Card.tsx
import React from 'react';
import styles from './Card.module.css';

export interface CardProps {
  children: React.ReactNode;
  title?: string;
  extra?: React.ReactNode;
  hoverable?: boolean;
  className?: string;
}

export const Card: React.FC<CardProps> = ({
  children,
  title,
  extra,
  hoverable = false,
  className = '',
}) => {
  return (
    <div className={`${styles.card} ${hoverable ? styles.hoverable : ''} ${className}`}>
      {(title || extra) && (
        <div className={styles.header}>
          {title && <h3 className={styles.title}>{title}</h3>}
          {extra && <div className={styles.extra}>{extra}</div>}
        </div>
      )}
      <div className={styles.body}>{children}</div>
    </div>
  );
};
```

## 🎨 样式转换规则

### Figma → CSS 属性映射

| Figma 属性 | CSS 属性 | 转换规则 |
|-----------|---------|---------|
| Fill (颜色) | `background-color` / `color` | 转换为 CSS 变量 |
| Stroke (描边) | `border` | 边框颜色和宽度 |
| Corner Radius | `border-radius` | 直接转换 |
| Effects (Shadow) | `box-shadow` | 转换为标准阴影值 |
| Opacity | `opacity` | 直接转换 |
| Blur | `filter: blur()` | 转换为滤镜 |
| Font Family | `font-family` | 映射到 Web 字体 |
| Font Size | `font-size` | 转换为 rem 或 px |
| Font Weight | `font-weight` | 直接转换 |
| Letter Spacing | `letter-spacing` | 直接转换 |
| Line Height | `line-height` | 转换为相对值或固定值 |

### Auto Layout 转换

```css
/* Figma: Auto Layout (Horizontal, Space Between) */
.flex-row {
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
}

/* Figma: Auto Layout (Vertical, Center) */
.flex-col {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

/* Figma: Auto Layout with Gap */
.flex-gap {
  display: flex;
  gap: var(--spacing-md);
}
```

## 🚀 代码生成优先级

1. **优先使用现有组件库**（如果已安装）
   - Ant Design: `<Button>`, `<Card>`, `<Input>` 等
   - Element Plus: `<el-button>`, `<el-card>` 等

2. **其次使用自定义通用组件**
   - 检查 `src/components/common/` 是否有可复用组件

3. **最后创建新组件**
   - 按照组件规范创建新的组件文件

## 📋 组件检查清单

生成组件后，检查以下项：
- [ ] 组件有完整的 TypeScript 类型定义
- [ ] Props 接口清晰明确
- [ ] 样式使用 CSS Modules
- [ ] 支持响应式设计
- [ ] 交互状态完整（hover, active, disabled）
- [ ] 可访问性属性已添加
- [ ] 组件可复用，无硬编码业务逻辑

