# Figma 转代码规则文档

## 📋 概述
本文档定义了从 Figma 设计稿转换为代码时需要遵循的规则和规范，参考主流后台管理系统（Ant Design Pro、Element Plus Admin 等）的最佳实践。

## 🏗️ 项目架构规范

### 1. 技术栈
- **框架**: React 18+ with TypeScript
- **构建工具**: Vite
- **UI 库**: 优先使用 Ant Design / Element Plus（如未安装，使用原生 HTML/CSS 实现）
- **样式方案**: CSS Modules + Tailwind CSS（可选）
- **状态管理**: React Hooks (useState, useReducer, Context API)

### 2. 文件组织结构
```
src/
├── components/          # 通用组件
│   ├── layout/         # 布局组件
│   ├── common/         # 通用组件
│   └── business/       # 业务组件
├── pages/              # 页面组件
│   └── [PageName]/
│       ├── index.tsx
│       ├── index.module.css
│       └── types.ts
├── hooks/              # 自定义 Hooks
├── utils/              # 工具函数
├── types/              # 类型定义
├── constants/          # 常量定义
├── assets/             # 静态资源
│   ├── images/
│   ├── icons/
│   └── fonts/
└── styles/             # 全局样式
    ├── variables.css   # CSS 变量
    ├── reset.css       # 重置样式
    └── common.css      # 通用样式
```

## 🎨 设计规范转换规则

### 1. 间距系统 (Spacing)
- Figma 间距值 → CSS 变量/常量
- 使用 4px 或 8px 的倍数
- 转换为间距变量：`--spacing-xs: 4px`, `--spacing-sm: 8px`, `--spacing-md: 16px` 等

### 2. 颜色系统 (Colors)
- 提取 Figma 中的颜色为 CSS 变量
- 颜色命名规范：`--color-primary`, `--color-success`, `--color-warning`, `--color-error`
- 支持明暗主题切换

### 3. 字体系统 (Typography)
- 字体大小：转换为 `--font-size-xs` 到 `--font-size-xl`
- 字重：`--font-weight-normal: 400`, `--font-weight-medium: 500`, `--font-weight-bold: 700`
- 行高：`--line-height-tight: 1.2`, `--line-height-normal: 1.5`

### 4. 圆角系统 (Border Radius)
- 统一圆角值：`--radius-sm: 4px`, `--radius-md: 8px`, `--radius-lg: 12px`, `--radius-full: 9999px`

### 5. 阴影系统 (Shadows)
- 转换为 CSS box-shadow
- 定义阴影层级：`--shadow-sm`, `--shadow-md`, `--shadow-lg`

## 🔧 组件开发规范

### 1. 组件命名
- **文件命名**: PascalCase，如 `UserCard.tsx`
- **组件命名**: PascalCase，与文件名一致
- **CSS 类名**: kebab-case，如 `user-card`

### 2. 组件结构模板
```tsx
// UserCard.tsx
import React from 'react';
import styles from './UserCard.module.css';

interface UserCardProps {
  name: string;
  avatar?: string;
  description?: string;
  onClick?: () => void;
}

export const UserCard: React.FC<UserCardProps> = ({
  name,
  avatar,
  description,
  onClick,
}) => {
  return (
    <div className={styles.card} onClick={onClick}>
      {avatar && <img src={avatar} alt={name} className={styles.avatar} />}
      <div className={styles.content}>
        <h3 className={styles.name}>{name}</h3>
        {description && <p className={styles.description}>{description}</p>}
      </div>
    </div>
  );
};
```

### 3. 组件分类规则

#### 基础组件 (Common Components)
- Button, Input, Card, Modal, Table, Form 等
- 位置：`src/components/common/`
- 要求：高复用性，无业务逻辑

#### 布局组件 (Layout Components)
- Header, Sidebar, Footer, Container, Grid 等
- 位置：`src/components/layout/`
- 要求：负责页面结构布局

#### 业务组件 (Business Components)
- 特定业务场景的组件
- 位置：`src/components/business/`
- 要求：可包含业务逻辑

## 📐 响应式设计规则

### 1. 断点定义
```css
--breakpoint-xs: 480px;
--breakpoint-sm: 768px;
--breakpoint-md: 1024px;
--breakpoint-lg: 1280px;
--breakpoint-xl: 1920px;
```

### 2. 移动端优先
- 使用 `min-width` 媒体查询
- 确保移动端基础样式完整

### 3. Grid 布局
- 使用 CSS Grid 或 Flexbox
- 栅格系统：12 列或 24 列

## 🎯 代码质量规范

### 1. TypeScript 规范
- **必须** 为所有 Props 定义接口
- **必须** 使用类型注解，避免 `any`
- **推荐** 使用联合类型和泛型

### 2. React 最佳实践
- 使用函数组件 + Hooks
- 组件职责单一
- Props 解构时提供默认值
- 使用 `React.memo` 优化性能（如需要）
- 事件处理函数使用 `useCallback`（如需要）

### 3. 样式规范
- **优先** 使用 CSS Modules
- 类名语义化，避免缩写
- 避免使用 `!important`
- 使用 CSS 变量统一管理样式

### 4. 可访问性 (A11y)
- 所有交互元素必须有 `aria-label` 或可见文本
- 图片必须有 `alt` 属性
- 表单元素必须有 `label`
- 支持键盘导航

### 5. 性能优化
- 图片使用 `loading="lazy"`
- 大列表使用虚拟滚动
- 避免不必要的重渲染
- 代码分割和懒加载

## 📝 命名规范

### 1. 变量命名
- **组件 Props**: camelCase，如 `userName`, `onClick`
- **CSS 类名**: kebab-case，如 `user-card`, `card-header`
- **常量**: UPPER_SNAKE_CASE，如 `MAX_ITEMS`, `API_BASE_URL`

### 2. 函数命名
- **事件处理**: `handle` + 事件名，如 `handleClick`, `handleSubmit`
- **工具函数**: 动词开头，如 `formatDate`, `validateEmail`
- **Hook**: `use` + 功能名，如 `useAuth`, `useLocalStorage`

## 🖼️ 资源处理规则

### 1. 图片资源
- **图标**: 优先使用 SVG，内联或作为组件
- **图片**: 导出为 WebP/PNG，放置在 `src/assets/images/`
- **命名**: kebab-case，如 `user-avatar.png`

### 2. 字体资源
- 放置在 `src/assets/fonts/`
- 使用 `@font-face` 定义

## 🚀 开发流程规则

### 1. Figma 元素识别
- **Text** → `<p>`, `<h1>-<h6>`, `<span>`
- **Rectangle** → `<div>` (如有交互) 或语义化标签
- **Image** → `<img>` 或 `<picture>`
- **Button** → `<button>` 或自定义 Button 组件
- **Input** → `<input>` 或自定义 Input 组件
- **Group/Frame** → `<div>` 或语义化组件

### 2. 代码生成步骤
1. 分析设计稿结构，识别组件层级
2. 提取设计变量（颜色、间距、字体等）
3. 创建组件文件结构
4. 实现组件 HTML 结构
5. 转换样式（Figma → CSS）
6. 添加交互逻辑（如需要）
7. 添加类型定义
8. 代码优化和重构

### 3. 代码审查清单
- [ ] 组件有完整的 TypeScript 类型定义
- [ ] 样式使用 CSS Modules 或 CSS 变量
- [ ] 响应式设计已实现
- [ ] 可访问性要求已满足
- [ ] 代码符合 ESLint 规范
- [ ] 无控制台错误或警告
- [ ] 性能优化已考虑

## 📚 参考标准
- [Ant Design 设计规范](https://ant.design/docs/spec/introduce-cn)
- [Element Plus 设计指南](https://element-plus.org/zh-CN/guide/design.html)
- [Material Design](https://material.io/design)
- [Web Content Accessibility Guidelines (WCAG)](https://www.w3.org/WAI/WCAG21/quickref/)

