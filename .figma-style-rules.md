# Figma 样式转换规则

## 🎨 设计 Token 转换

### 1. 颜色系统 (Color Tokens)

#### 主色系提取规则
```css
/* 从 Figma 提取颜色，转换为 CSS 变量 */
:root {
  /* Primary Colors - 主色调 */
  --color-primary: #3E7BFA;
  --color-primary-hover: #2E6BEA;
  --color-primary-active: #1E5BDA;
  --color-primary-light: #E8F0FF;
  
  /* Success Colors - 成功色 */
  --color-success: #06C270;
  --color-success-light: #E6F9F0;
  
  /* Warning Colors - 警告色 */
  --color-warning: #FF8800;
  --color-warning-light: #FFF4E6;
  
  /* Error Colors - 错误色 */
  --color-error: #FF3B3B;
  --color-error-light: #FFE6E6;
  
  /* Neutral Colors - 中性色 */
  --color-text-primary: #1C1C28;
  --color-text-secondary: #555770;
  --color-text-tertiary: #A9A9AF;
  --color-text-disabled: #DADCE1;
  
  /* Background Colors */
  --color-bg-primary: #FFFFFF;
  --color-bg-secondary: #F6F7FB;
  --color-bg-tertiary: #F5F5F5;
  
  /* Border Colors */
  --color-border: #DADCE1;
  --color-border-light: #F0F0F0;
}
```

#### 颜色识别规则
- **主色**: 用于按钮、链接等主要交互元素
- **成功色**: 通常为绿色系，用于成功提示
- **警告色**: 通常为橙色系，用于警告提示
- **错误色**: 通常为红色系，用于错误提示
- **中性色**: 用于文本、边框、背景等

### 2. 间距系统 (Spacing Tokens)

```css
:root {
  /* 基于 4px 基础单位 */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  --spacing-2xl: 48px;
  --spacing-3xl: 64px;
}

/* 使用示例 */
.card {
  padding: var(--spacing-md);
  margin-bottom: var(--spacing-lg);
  gap: var(--spacing-sm);
}
```

### 3. 字体系统 (Typography Tokens)

```css
:root {
  /* 字体大小 */
  --font-size-xs: 12px;
  --font-size-sm: 14px;
  --font-size-base: 16px;
  --font-size-lg: 18px;
  --font-size-xl: 20px;
  --font-size-2xl: 24px;
  --font-size-3xl: 28px;
  --font-size-4xl: 32px;
  
  /* 字重 */
  --font-weight-normal: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;
  --font-weight-bold: 700;
  
  /* 行高 */
  --line-height-tight: 1.2;
  --line-height-normal: 1.5;
  --line-height-relaxed: 1.75;
  
  /* 字体系列 */
  --font-family-base: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Microsoft YaHei UI', sans-serif;
  --font-family-mono: 'Consolas', 'Monaco', monospace;
}
```

### 4. 圆角系统 (Border Radius Tokens)

```css
:root {
  --radius-none: 0;
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 12px;
  --radius-xl: 16px;
  --radius-full: 9999px;
}
```

### 5. 阴影系统 (Shadow Tokens)

```css
:root {
  --shadow-sm: 0px 1px 2px 0px rgba(0, 0, 0, 0.05);
  --shadow-md: 0px 4px 8px 0px rgba(0, 0, 0, 0.1);
  --shadow-lg: 0px 8px 16px 0px rgba(0, 0, 0, 0.15);
  --shadow-xl: 0px 12px 24px 0px rgba(0, 0, 0, 0.2);
  
  /* 特殊阴影 */
  --shadow-card: 0px 2px 8px 0px rgba(96, 97, 112, 0.16);
  --shadow-dropdown: 0px 4px 12px 0px rgba(0, 0, 0, 0.15);
}
```

### 6. 过渡动画 (Transition Tokens)

```css
:root {
  --transition-fast: 150ms ease-in-out;
  --transition-base: 200ms ease-in-out;
  --transition-slow: 300ms ease-in-out;
}
```

## 📐 布局转换规则

### 1. Flexbox 布局
```css
/* Figma: Auto Layout Horizontal */
.flex-row {
  display: flex;
  flex-direction: row;
  align-items: center;
}

/* Figma: Auto Layout Vertical */
.flex-col {
  display: flex;
  flex-direction: column;
}

/* Figma: Space Between */
.flex-between {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* Figma: Center */
.flex-center {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

### 2. Grid 布局
```css
/* Figma: Grid Layout */
.grid {
  display: grid;
  grid-template-columns: repeat(12, 1fr);
  gap: var(--spacing-md);
}

/* 响应式 Grid */
.grid-responsive {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: var(--spacing-md);
}
```

### 3. 定位系统
```css
/* Figma: Absolute Position */
.absolute {
  position: absolute;
  top: 0;
  left: 0;
}

/* Figma: Fixed Position */
.fixed {
  position: fixed;
  top: 0;
  right: 0;
}
```

## 🎯 响应式断点

```css
/* 移动端优先 - 使用 min-width */
@media (min-width: 480px) {
  /* 小屏幕设备 */
}

@media (min-width: 768px) {
  /* 平板设备 */
}

@media (min-width: 1024px) {
  /* 桌面设备 */
}

@media (min-width: 1280px) {
  /* 大桌面设备 */
}

@media (min-width: 1920px) {
  /* 超大屏幕 */
}
```

## 🔄 Figma → CSS 属性映射表

### 视觉效果转换

| Figma 效果 | CSS 实现 |
|-----------|---------|
| Drop Shadow | `box-shadow: 0px 4px 8px rgba(0,0,0,0.1)` |
| Inner Shadow | `box-shadow: inset 0px 2px 4px rgba(0,0,0,0.1)` |
| Layer Blur | `filter: blur(10px)` |
| Background Blur | `backdrop-filter: blur(10px)` |
| Gradient Fill | `background: linear-gradient(...)` |

### 文本样式转换

```css
/* Figma Text Style → CSS Class */
.text-heading-1 {
  font-size: var(--font-size-4xl);
  font-weight: var(--font-weight-bold);
  line-height: var(--line-height-tight);
  color: var(--color-text-primary);
}

.text-body {
  font-size: var(--font-size-base);
  font-weight: var(--font-weight-normal);
  line-height: var(--line-height-normal);
  color: var(--color-text-secondary);
}

.text-caption {
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-normal);
  line-height: var(--line-height-normal);
  color: var(--color-text-tertiary);
}
```

## 📱 移动端适配规则

### 1. 字体缩放
```css
/* 基础字体大小 */
html {
  font-size: 16px;
}

/* 移动端适配 */
@media (max-width: 768px) {
  html {
    font-size: 14px;
  }
}

/* 使用 rem 单位 */
.text {
  font-size: 1rem; /* 16px on desktop, 14px on mobile */
}
```

### 2. 间距缩放
```css
/* 移动端减少间距 */
.card {
  padding: var(--spacing-md);
}

@media (max-width: 768px) {
  .card {
    padding: var(--spacing-sm);
  }
}
```

### 3. 布局调整
```css
/* 桌面端：水平布局 */
.grid {
  grid-template-columns: repeat(3, 1fr);
}

/* 移动端：单列布局 */
@media (max-width: 768px) {
  .grid {
    grid-template-columns: 1fr;
  }
}
```

## 🎨 CSS Modules 使用规范

### 文件命名
- 组件样式：`ComponentName.module.css`
- 全局样式：`global.css` 或 `common.css`

### 类名规范
```css
/* ✅ 推荐：语义化命名 */
.card {
  background: var(--color-bg-primary);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-card);
}

.card-header {
  padding: var(--spacing-md);
  border-bottom: 1px solid var(--color-border);
}

.card-body {
  padding: var(--spacing-md);
}

/* ❌ 避免：无意义命名 */
.a1 {
  background: white;
}

.b2 {
  padding: 16px;
}
```

## 🔍 样式优化建议

### 1. 性能优化
```css
/* ✅ 使用 transform 而非 position */
.hover-lift {
  transition: transform var(--transition-base);
}

.hover-lift:hover {
  transform: translateY(-2px);
}

/* ❌ 避免频繁触发重排 */
.bad-hover {
  transition: top var(--transition-base);
}

.bad-hover:hover {
  top: -2px;
}
```

### 2. 可维护性
```css
/* ✅ 使用 CSS 变量 */
.button {
  background: var(--color-primary);
  color: var(--color-text-inverse);
}

/* ❌ 避免硬编码 */
.bad-button {
  background: #3E7BFA;
  color: #FFFFFF;
}
```

### 3. 浏览器兼容性
```css
/* 使用标准属性，提供回退方案 */
.flex {
  display: flex;
  display: -webkit-flex; /* 旧版 Safari */
}
```

## 📋 样式检查清单

生成样式后，检查：
- [ ] 使用 CSS 变量而非硬编码值
- [ ] 响应式设计已实现
- [ ] 支持暗色模式（如需要）
- [ ] 浏览器兼容性已考虑
- [ ] 性能优化已应用
- [ ] 可访问性（对比度、焦点状态）已满足

